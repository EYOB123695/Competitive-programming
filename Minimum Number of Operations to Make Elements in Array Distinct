class Solution:
    def minimumOperations(self, nums: List[int]) -> int:
        answer =  0 
       
        while len(set(nums)) != len(nums) :
            nums = nums[3:]
            answer +=1 
        return answer
            
        


        
