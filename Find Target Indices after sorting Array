class Solution(object):
    def targetIndices(self, nums, target):
        """
        :type nums: List[int]
        :type target: int
        :rtype: List[int]
        """
        ans=[]
        nums.sort()
        for i, num in enumerate(nums):
            if (num==target) :
                ans.append(i)
        return ans

        
